import type {
  Callback,
  IdentityCallback,
  NumberCallback,
  ReducerCallback,
} from './Callback'
import identity from './identity'

export type { Callback, IdentityCallback, NumberCallback, ReducerCallback }
export { identity }
